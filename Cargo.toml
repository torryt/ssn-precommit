[package]
name = "ssn-precommit"
version = "0.1.0"
edition = "2024"
description = "Pre-commit hook to detect 11-digit SSNs in staged changes"

[[bin]]
name = "ssn-precommit"
path = "src/main.rs"

[dependencies]
regex = "1"
